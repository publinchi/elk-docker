input {
  beats {
    port => 5044
    codec => multiline {
      pattern => "^%{WORD:fecha} %{WORD:hora},%{WORD:ms} "
      negate => true
      what => previous
    }
  }
}

input {
  beats {
    port => 5045
  }
}

input {
  tcp {
    port => 5555
    codec => multiline {
      pattern => "^%{WORD:fecha} %{WORD:hora},%{WORD:ms} "
      negate => true
      what => previous
    }
    type => "CTS-Loaded"
  }
}
